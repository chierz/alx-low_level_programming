#include "main.h"

/**
 *main - entry point
 *Return: return 1 if c is a capital letter
 *otherwise Return 0
 */
int _isupper(int c)
{
	if (c => 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
