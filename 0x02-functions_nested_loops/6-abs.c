#include "main.h"

/**
 *_abs - computes absolute value iow makes all nums positive if they are not
 *@n: the number
 *Return: retvalue
 */

int _abs(int n)

{
	if (n < 0)
		return (-n);
	else
		return (n);
}

