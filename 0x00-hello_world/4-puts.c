#include <stdio.h>
/**
  *main - entry point
  *puts - prints string to screen
  *return - always 0 if (success) and a value greater if fail
  */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
